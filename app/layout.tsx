import "./globals.css";
import type { Metadata } from "next";
import { Inter } from "next/font/google";
import { Providers } from "./providers";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Classroom Quiz Platform - Live Learning & Assessments",
  description: "Production-grade live classroom platform with real-time quizzes, attendance tracking, and analytics.",
  keywords: ["classroom", "quiz", "education", "learning", "online classes", "live quiz"],
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={inter.className}>
        <Providers>{children}</Providers>
      </body>
    </html>
  );
}
